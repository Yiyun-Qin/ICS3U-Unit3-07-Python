GET age
TRY
    age_int = int(age)
    IF (age_int > 25 and age_int < 40) THEN
        SHOW "You are an acceptable age."
    ELSE
        SHOW "You are NOT an acceptable age!"
    ENDIF
EXCEPT Exception
    SHOW "age is not a valid answer!"
FINALLY
    SHOW "Done."
ENDTRY